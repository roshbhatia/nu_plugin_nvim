# Open structured data in a scratch buffer

This recipe renders the current directory listing in a scratch buffer.

```sh
nu recipes/scratch-data/main.nu
```

The scratch buffer is unlisted and has no backing file.
